const capitalize = (string) => {
  return string.charAt(0).toUpperCase() + string.slice(1); // capitalizes only first letter of string and leaves the rest lower case
}
export { capitalize };
const foo = "bar";
export { foo };
const bar = "foo";
export { bar };
